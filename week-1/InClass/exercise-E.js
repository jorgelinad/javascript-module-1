const saludo = "Hola buenas noches";

const nombre = "mi nombre es Jorgelina"

const concatenar = saludo+ " " +nombre;

const interpolacion = `${saludo} ${nombre}`;

console.log(concatenar);
console.log(interpolacion);